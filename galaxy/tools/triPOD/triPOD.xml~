<tool id="triPOD" name="Identify mosaic abnormalities in a trio using parent of origin detection">
  <description>for each file containing a trio of SNP data</description>
  <command interpreter="perl">triPOD.pl --nograph --out=results $input</command>
  <inputs>
    <param format="tabular" name="input" type="data" label="Source file"/>
  </inputs>
  <outputs>
    <data format="tabular" name="results/triPOD_Results.txt" />
  </outputs>

  <tests>
    <test>
      <param name="input" value="my_UPD_trio_SNPdata.txt"/>
      <output name="out_file1" file="results/triPOD_Results.txt"/>
    </test>
  </tests>

  <help>
This tool detects mosaic abnormalities when supplied with a trio of SNP data. See http://pevsnerlab.kennedykrieger.org for information about local usage and licensing.
  </help>

</tool>
